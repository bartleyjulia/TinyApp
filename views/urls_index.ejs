<!DOCTYPE html>
<html lang="en">
<head>
   <% include ./partials/_header%>
  <title>TinyApp</title>
</head>
<body>


<% for (const shortURL in urls){ %>

<div>
<% if (!userID) { %>

  <%= shortURL %>

<% } %>
<% } %>


<% if (userID) { %>

<% for (const data in usersURLs) { %>

<%= data %>

<%= usersURLs[data] %>


<form method="POST" action="/urls/<%= data %>/delete">

  <button type='submit'>🗑</form>
</div>

<form action="/urls/<%= data %>" method="GET">
      <input type="submit" value="Update">
    </form>


<% } %>
<% } %>

<form action="/urls/new " method="GET">
      <input type="submit" value="NewURL">
    </form>
</body>
</html>